require 'helper'

describe Faraday::Request do
  context "when making a request" do

#    before do
#      stub_get('search/wines').
#        with(:query => {:fmt => Vintank.format, :q => 'rodney'}).
#        to_return(:body => fixture("wines.json"), :headers => {:content_type => "application/json; charset=utf-8"})
#    end
#
#    it "should include Authorization header" do
#      Vintank.wine_search('rodney')
#      a_get("search/wines").
#        with(:query => {:fmt => Vintank.format, :q => "rodney"},
#             :headers => {"Authorization" => "Vintank appId="", sig="", timestamp="", uri="}).
#        should have_been_made
#    end
  end
end
